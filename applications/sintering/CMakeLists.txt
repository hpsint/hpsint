
# Sintering 2D, max 5 particles (circle)
ADD_EXECUTABLE(sintering-circle-2D "sintering_circle.cc")
DEAL_II_SETUP_TARGET(sintering-circle-2D)
TARGET_COMPILE_DEFINITIONS(sintering-circle-2D PUBLIC -DSINTERING_DIM=2 -DMAX_SINTERING_GRAINS=5)
TARGET_LINK_LIBRARIES(sintering-circle-2D "pf-applications")
target_include_directories(sintering-circle-2D PUBLIC "include/")

# Sintering 3D, max 5 particles (circle)
ADD_EXECUTABLE(sintering-circle-3D "sintering_circle.cc")
DEAL_II_SETUP_TARGET(sintering-circle-3D)
TARGET_COMPILE_DEFINITIONS(sintering-circle-3D PUBLIC -DSINTERING_DIM=3 -DMAX_SINTERING_GRAINS=5)
TARGET_LINK_LIBRARIES(sintering-circle-3D "pf-applications")
target_include_directories(sintering-circle-3D PUBLIC "include/")

# Sintering 2D, max 10 particles (cloud)
ADD_EXECUTABLE(sintering-cloud-2D "sintering_cloud.cc")
DEAL_II_SETUP_TARGET(sintering-cloud-2D)
TARGET_COMPILE_DEFINITIONS(sintering-cloud-2D PUBLIC -DSINTERING_DIM=2 -DMAX_SINTERING_GRAINS=10)
TARGET_LINK_LIBRARIES(sintering-cloud-2D "pf-applications")
target_include_directories(sintering-cloud-2D PUBLIC "include/")
